#include <stdlib.h>
#include <stdio.h>

int main(int argc, char* argv[])
{
  system(argv[1]);
  printf("done tata bye!!");
  exit(0);
}

// example to run
// ./a.out pwd
// then it will run the pwd command
